"""
Pytest configuration and fixtures.

This module provides pytest configuration for VibeCForms tests.

Configuration:
    - Temporary test files are created in data/txt/tests/ via --basetemp
    - This keeps data/txt/ clean and organized
    - All pytest temp directories are automatically cleaned up after tests
"""
